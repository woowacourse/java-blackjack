# java-blackjack

블랙잭 미션 저장소
## 기능 구현 목록

## 정한 규칙
- 덱은 1개로 제한한다. (카드 56장 중복 x)
- 첫 카드합이 21이면 블랙잭이 되었음을 알린다.
- 21이 될경우 추가로 카드를 받지 않는다.
- A는 버스트일 경우 1로 계산되고 아닐 경우 11로 계산된다.
    - 3개 이상일 경우 테스트 케이스 생각해봐야함.


### 유저
- 이름을 저장한다.
  - 1~7자이다.
  - 영문만 입력받는다.
- 블랙잭 핸드를 관리한다.

### 딜러
- 이름과, 블랙잭 핸드를 관리한다.
- 핸드 값이 16 이하라면 카드를 받아야 함을 알린다.(자동)

### 블랙잭핸드
- 받은 카드를 저장한다.
  - A의 1or11 처리를 한다.    **주의**
- 카드의 숫자를 반환한다. (J/Q/K - 10)
- 카드의 총 개수를 반환한다.
- 버스트인지 확인하다 (21 초과하는지 확인한다.)

### 덱
-[x] 52장의 카드를 저장한다.
    - 카드를 리스트로 관리한다.
-[x] 랜덤값을 주입받아 카드를 전달한다.
  - 카드는 삭제된다. (LinkedList 로 구현해서 O(1)) 
  - 랜덤값은 0 ~ (size-1) 까지이다.
-[x] 카드의 사이즈를 전달한다.
-[x] 카드가 있는지 확인한다(동치)

### 카드관리
-[x] ENUM(문양), ENUM(숫자, JKQ, A)로 각각 저장한다. 

### 카드 생성기
-[x] ENUM(문양), ENUM(숫자, J/K/Q, A) 두 ENUM 각각을 조합해서 56개의 카드를 생성한다. - 카드 객체

### 카드
-[x] 문양, 숫자

### 입출력
- 플레이어 이름은 `,`로 구분한다.


# 페어프로그래밍 기록
- 테스트에서 자주 사용되는 값을 TestDefaults 통해서 관리한다.
  이유: 다양한 클래스에서 자주 사용하기 때문에. 

- 랜덤값 테스트가 어려워서 (덱셔플 -> 덱의 idx 값을 가져오도록 변경)
  - 추후 idx 값을 주입받는다.
  - idx 값이 삭제되기 때문에 LinkedList 사용 O(1)