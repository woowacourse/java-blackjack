# java-blackjack

블랙잭 미션 저장소

## 프로그램 설명

Java로 구현한 콘솔 기반 블랙잭 게임입니다.

### 게임 흐름

1. **플레이어 이름 입력** - 쉼표(`,`)로 구분하여 여러 명의 플레이어 이름을 입력합니다.
2. **플레이어 배팅 금액 입력** - 각 플레이어마다 양의 정수로 배팅 금액을 입력합니다.
3. **초기 카드 배분** - 딜러와 모든 플레이어에게 각 2장의 카드를 나눠줍니다. 딜러는 첫 번째 카드 1장만 공개합니다.
4. **플레이어 턴** - 각 플레이어는 카드를 추가로 뽑을지 여부를 `y/n`으로 선택합니다. 21점을 초과하면 버스트(패배)됩니다.
5. **딜러 턴** - 딜러는 16점 이하이면 자동으로 카드를 추가로 뽑습니다.
6. **최종 결과 출력** - 모든 참가자의 최종 카드와 점수, 조건에 따른 최종 수익을 출력합니다.

### 프로젝트 구조

```
src/main/java/
├── Main.java                     # 진입점
├── controller/
│   └── BlackJackController       # 게임 흐름 제어
├── model/
│   ├── card
│   │     ├── BlackJackDeck       # 게임에 사용할 카드
│   │     ├── Card                # 카드
│   │     ├── CardNumber          # 카드 번호
│   │     ├── Cards               # 현재 갖고 있는 카드 관리
│   │     └── Shape               # 딜러/플레이어 공통 부모 클래스
│   ├── participant
│   │     ├── Agreement           # 참가자 드로우 여부 입력 관리
│   │     ├── BetPrice            # 참가자 배팅 금액 입력 관리
│   │     ├── Dealer              # 딜러
│   │     ├── DealerStatus        # 딜러 상태(이름, 점수, 버스트 여부, 블랙잭 여부) 관리
│   │     ├── Participant         # 참가자(플레이어, 딜러)
│   │     ├── ParticipantHand     # 참가자 손패(점수, 카드)
│   │     ├── Participants        # 게임의 딜러, 플레이어들 관리
│   │     ├── Player              # 플레이어
│   │     ├── PlayerName          # 플레이어 이름 관리
│   │     ├── Players             # 참가자 생성 및 등록 관리
│   │     ├── PlayerStatus        # 플레이어 상태(이름, 점수, 배팅 금액, 버스트 여부, 블랙잭 여부) 관리
│   │     └── Score               # 현재 점수 관리
│   ├── result
│   │     ├── DealerProfit        # 딜러 수익금 관리
│   │     ├── ParticipantProfit   # 참가자 수익 저장
│   │     ├── PlayerProfits       # 플레이어들 수익 저장
│   │     ├── ProfitCalculator    # 수익 계산
│   │     └── ProfitResult        # 딜러, 참가자 수익 관리
│   └── BlackJackGame             # 블랙잭 게임 관리
├── dto/                          # 데이터 전달 객체
└── view/
    ├── InputView                 # 입력 뷰
    └── OutputView                # 출력 뷰
```

### 주요 규칙

- **버스트**: 카드 합산 점수가 21을 초과하면 자동 패배
- **블랙잭**: 참가자 카드가 2장이면서, 합이 21인 경우
- **A(에이스)**: 카드 현재 점수에 따라 1 또는 11로 적용가능
- **J, Q, K**: 10점으로 계산
- **딜러**: 16점 이하일 경우 자동으로 카드를 추가 드로우
- **플레이어 이름**: 중복 불가, `딜러`라는 이름 사용 불가

### 기능 구현 목록

- 딜러
  - [x] 처음 카드를 두 장 받는다.
  - [x] 카드를 한장만 공개한다. (제약 : 처음만 공개)
  - [x] 두 카드의 합이 16이하일때 카드를 한장 더 받는다.

- 플레이어
  - [x] 처음에 카드를 두 장 받는다.
  - [x] 배팅 금액을 입력받는다.
  - [x] 카드를 두장 전부 공개한다.
  - [x] 카드의 합이 21을 넘지 않을 경우, 카드를 뽑을 수 있다.
  - [x] 플레이어의 이름은 겹치면 안된다.
  - [x] 플레이어는 한명 이상이어야 한다.
  - [x] 플레이어는 딜러라는 이름을 쓰면 안된다.
  
- 랜덤 카드 생성
  - [x] 카드는 2~9,J,Q,K,A의 넘버링을 가진다.
  - [x] 카드는 하트, 다이아몬드, 스페이드, 클로버의 문양을 가진다.
  - [x] 카드는 중복되면 안된다.

- 점수 계산
  - [x] 카드는 숫자 넘버링일 경우 그 숫자로, J,K,Q의 넘버링일때는 10점의 점수를 가진다.
  - [x] A는 카드에 따라 1 또는 11의 점수를 가질 수 있다.
    - [x] A가 없을 경우, 카드의 합이 10이하일 경우에는 11점, 11점 이상일때는 1점을 가진다.
    - [x] A가 이전에 있을경우 그 A는 무조건 1점을 가진다.
  - [x] 플레이어의 점수는 카드의 합산 점수로 결정된다.

- 수익 계산
  - [x] 참가자가 버스트인 경우, 바로 배팅금액의 음수로 처리한다.
  - [x] 참가자가 블랙잭인 경우
    - [x] 딜러가 블랙잭인 경우, 0으로 계산한다.
    - [x] 플레이어만 블랙잭인 경우, 1.5배로 계산한다.
  - [x] 딜러가 버스트이거나 플레이어보다 점수가 낮은 경우, 배팅 금액으로 처리한다.
  - [x] 딜러보다 낮은 점수인 경우, 배팅금액의 음수로 처리한다.
  - [x] 점수가 같은 경우, 0으로 계산한다.

- 블랙잭, 버스트 판정
  - [x] 21점을 넘으면 버스트로 판정된다.
  - [x] 카드가 2장이면서 21점인 경우 블랙잭으로 판정된다.

- 입력
  - [x] 플레이어 이름을 입력한다.
    - [x] 이름은 쉼표 단위로 구분한다.
  - [x] 배팅 금액을 입력한다.
    - [x] 배팅 금액은 0이상 양의 정수로 입력한다.
  - [x] 플레이어가 카드를 뽑고 싶을 때 y,n만을 입력한다.
    - [x] 다른 문자가 들어왔을 때 예외를 뱉는다.

- 출력
  - [x] 딜러의 첫번째 카드를 출력한다.
  - [x] 플레이어의 카드 덱을 출력한다.
    - [x] 플레이어가 카드를 뽑을 경우 갱신된 카드 덱을 출력한다.
  - [x] 딜러가 카드를 뽑았는지의 여부를 출력한다.
  - [x] 최종덱과 총 점수를 출력한다.
  - [x] 최종 수익을 출력한다.