package blackjack.domain;

public enum ResultType {
    WIN("승"),
    TIE("무"),
    LOSE("패");

    public ResultType getOppositeResult() {
        if (this == WIN) {
            return LOSE;
        }
        if (this == TIE) {
            return TIE;
        }
        return WIN;
    }
}
